# راهنمای کامل Linter، Formatter و Commit Hooks

این مستند توضیح می‌دهد که چگونه Linter، Formatter و Git Hooks در این پروژه راه‌اندازی شده‌اند.

---

## 📋 فهرست مطالب

1. [ESLint (Linter)](#eslint-linter)
2. [Prettier (Formatter)](#prettier-formatter)
3. [Husky (Git Hooks)](#husky-git-hooks)
4. [lint-staged](#lint-staged)
5. [نحوه استفاده](#نحوه-استفاده)
6. [پیکربندی‌ها](#پیکربندی‌ها)

---

## 🔍 ESLint (Linter)

### ESLint چیست؟

ESLint یک ابزار تحلیل کد استاتیک است که مشکلات و خطاهای احتمالی در کد JavaScript/TypeScript را پیدا می‌کند. این ابزار به شما کمک می‌کند:

- **کیفیت کد را حفظ کنید**: خطاهای رایج را قبل از اجرا پیدا می‌کند
- **استایل کد را یکپارچه کنید**: قوانین یکسانی برای همه اعمال می‌شود
- **بهترین روش‌ها را رعایت کنید**: الگوهای بد را شناسایی می‌کند

### پکیج‌های نصب شده

```json
{
  "eslint": "^9.39.2", // هسته ESLint
  "@typescript-eslint/parser": "^8.50.0", // پارسر TypeScript
  "@typescript-eslint/eslint-plugin": "^8.50.0", // پلاگین TypeScript
  "eslint-plugin-vue": "^10.6.2", // پلاگین Vue 3
  "eslint-config-prettier": "^10.1.8", // غیرفعال کردن قوانین ESLint که با Prettier تداخل دارند
  "eslint-plugin-prettier": "^5.5.4" // اجرای Prettier به عنوان یک قانون ESLint
}
```

### فایل پیکربندی: `.eslintrc.cjs`

این فایل شامل:

1. **Parser**: `vue-eslint-parser` برای فایل‌های Vue و `@typescript-eslint/parser` برای TypeScript
2. **Extends**: قوانین پیش‌فرض از:
   - `eslint:recommended` - قوانین پیشنهادی ESLint
   - `plugin:vue/vue3-recommended` - قوانین Vue 3
   - `plugin:@typescript-eslint/recommended` - قوانین TypeScript
   - `plugin:prettier/recommended` - ادغام با Prettier

3. **قوانین سفارشی**:
   - `vue/multi-word-component-names: "off"` - اجازه نام‌های تک کلمه‌ای برای کامپوننت‌ها
   - `@typescript-eslint/no-unused-vars` - هشدار برای متغیرهای استفاده نشده
   - `no-console` - فقط در production هشدار می‌دهد
   - و موارد دیگر...

### فایل‌های نادیده گرفته شده

ESLint این پوشه‌ها را بررسی نمی‌کند:

- `.nuxt` - فایل‌های تولید شده Nuxt
- `.output` - خروجی build
- `node_modules` - پکیج‌های نصب شده
- `dist` - فایل‌های build شده

---

## 💅 Prettier (Formatter)

### Prettier چیست؟

Prettier یک فرمت‌کننده کد است که به صورت خودکار کد شما را فرمت می‌کند. این ابزار:

- **استایل یکپارچه**: همه کدها با یک استایل فرمت می‌شوند
- **صرفه‌جویی در زمان**: دیگر نیازی به بحث در مورد استایل کد نیست
- **خودکار**: می‌تواند به صورت خودکار اجرا شود

### فایل پیکربندی: `.prettierrc`

```json
{
  "semi": true, // استفاده از semicolon در انتهای خط
  "singleQuote": false, // استفاده از double quotes به جای single
  "tabWidth": 2, // 2 space برای هر tab
  "trailingComma": "es5", // trailing comma در جاهای مجاز ES5
  "printWidth": 100, // حداکثر 100 کاراکتر در هر خط
  "arrowParens": "always", // همیشه پرانتز برای arrow functions
  "endOfLine": "lf", // استفاده از LF به جای CRLF
  "bracketSpacing": true, // فاصله در object literals: { foo: bar }
  "vueIndentScriptAndStyle": false // عدم indent در script و style تگ‌های Vue
}
```

### فایل نادیده گرفته شده: `.prettierignore`

Prettier این فایل‌ها را فرمت نمی‌کند:

- `node_modules` - پکیج‌های نصب شده
- فایل‌های lock (pnpm-lock.yaml, package-lock.json)
- پوشه‌های build (`.nuxt`, `.output`, `dist`)
- فایل‌های log

---

## 🪝 Husky (Git Hooks)

### Husky چیست؟

Husky ابزاری است که Git Hooks را در پروژه Node.js مدیریت می‌کند. Git Hooks اسکریپت‌هایی هستند که در رویدادهای خاص Git (مثل commit، push) اجرا می‌شوند.

### چرا از Husky استفاده می‌کنیم؟

- **کیفیت کد**: قبل از commit، کد بررسی می‌شود
- **جلوگیری از commit کد بد**: اگر کد مشکل داشته باشد، commit انجام نمی‌شود
- **اتوماتیک**: نیازی به یادآوری دستی نیست

### پیکربندی

1. **در `package.json`**:

   ```json
   {
     "scripts": {
       "prepare": "husky"
     }
   }
   ```

   این اسکریپت بعد از `pnpm install` اجرا می‌شود و Husky را راه‌اندازی می‌کند.

2. **Hook ایجاد شده: `.husky/pre-commit`**

   ```bash
   #!/usr/bin/env sh
   . "$(dirname -- "$0")/_/husky.sh"

   pnpm lint-staged
   ```

   این hook قبل از هر commit اجرا می‌شود و `lint-staged` را صدا می‌زند.

### نحوه کار

1. شما `git commit` می‌کنید
2. Husky hook `pre-commit` را اجرا می‌کند
3. `lint-staged` فقط فایل‌های staged را بررسی می‌کند
4. اگر مشکلی باشد، commit انجام نمی‌شود
5. اگر همه چیز درست باشد، commit انجام می‌شود

---

## 🎯 lint-staged

### lint-staged چیست؟

`lint-staged` فقط فایل‌هایی که در staging area هستند (با `git add` اضافه شده‌اند) را بررسی می‌کند. این کار:

- **سریع‌تر است**: فقط فایل‌های تغییر یافته بررسی می‌شوند
- **کارآمدتر است**: نیازی به بررسی کل پروژه نیست
- **متمرکز است**: فقط روی تغییرات فعلی تمرکز می‌کند

### پیکربندی در `package.json`

```json
{
  "lint-staged": {
    "*.{js,jsx,ts,tsx,vue}": [
      "eslint --fix", // اجرای ESLint و رفع خودکار مشکلات
      "prettier --write" // فرمت کردن با Prettier
    ],
    "*.{json,css,scss,md}": [
      "prettier --write" // فقط فرمت کردن (بدون lint)
    ]
  }
}
```

### نحوه کار

1. شما چند فایل را تغییر می‌دهید
2. با `git add` آن‌ها را به staging اضافه می‌کنید
3. `git commit` می‌کنید
4. `lint-staged` فقط فایل‌های staged را:
   - با ESLint بررسی می‌کند و مشکلات را رفع می‌کند
   - با Prettier فرمت می‌کند
5. اگر همه چیز درست باشد، commit انجام می‌شود

---

## 🚀 نحوه استفاده

### دستورات موجود

#### 1. بررسی کد با ESLint

```bash
# بررسی همه فایل‌ها
pnpm lint

# بررسی و رفع خودکار مشکلات
pnpm lint:fix
```

#### 2. فرمت کردن کد با Prettier

```bash
# فرمت کردن همه فایل‌ها
pnpm format

# بررسی اینکه آیا فایل‌ها فرمت شده‌اند یا نه (بدون تغییر)
pnpm format:check
```

### جریان کار پیشنهادی

#### روش 1: استفاده از Commit Hooks (پیشنهادی)

1. کد خود را بنویسید
2. فایل‌ها را با `git add` اضافه کنید
3. `git commit -m "your message"` بزنید
4. به صورت خودکار:
   - ESLint مشکلات را بررسی و رفع می‌کند
   - Prettier کد را فرمت می‌کند
5. اگر مشکلی باشد، commit انجام نمی‌شود و باید مشکلات را رفع کنید
6. دوباره commit کنید

#### روش 2: اجرای دستی

```bash
# قبل از commit، دستی اجرا کنید
pnpm lint:fix
pnpm format
git add .
git commit -m "your message"
```

### اگر Commit انجام نشد

اگر commit شما انجام نشد، احتمالاً:

1. **خطای ESLint**: کدی که نمی‌تواند به صورت خودکار رفع شود
   - خطا را در ترمینال ببینید
   - آن را دستی رفع کنید
   - دوباره commit کنید

2. **خطای فرمت**: Prettier نمی‌تواند فایل را فرمت کند
   - بررسی کنید که فایل معتبر است
   - مشکلات syntax را رفع کنید

### رد کردن Hooks (توصیه نمی‌شود)

اگر واقعاً نیاز دارید hook را رد کنید:

```bash
git commit --no-verify -m "your message"
```

⚠️ **هشدار**: این کار را فقط در موارد ضروری انجام دهید!

---

## ⚙️ پیکربندی‌ها

### تغییر قوانین ESLint

فایل `.eslintrc.cjs` را ویرایش کنید:

```javascript
rules: {
  // اضافه کردن قانون جدید
  "your-rule": "error",

  // تغییر قانون موجود
  "no-console": "off", // به جای "warn"
}
```

### تغییر تنظیمات Prettier

فایل `.prettierrc` را ویرایش کنید:

```json
{
  "printWidth": 120, // تغییر حداکثر طول خط
  "tabWidth": 4, // تغییر اندازه tab
  "singleQuote": true // استفاده از single quotes
}
```

### تغییر lint-staged

در `package.json` بخش `lint-staged` را ویرایش کنید:

```json
{
  "lint-staged": {
    "*.vue": ["eslint --fix", "prettier --write"],
    "*.ts": ["eslint --fix"]
  }
}
```

### اضافه کردن Hook جدید

```bash
# ایجاد hook جدید (مثلاً pre-push)
pnpm exec husky add .husky/pre-push "pnpm lint"
```

---

## 📚 منابع بیشتر

- [ESLint Documentation](https://eslint.org/docs/latest/)
- [Prettier Documentation](https://prettier.io/docs/en/)
- [Husky Documentation](https://typicode.github.io/husky/)
- [lint-staged Documentation](https://github.com/lint-staged/lint-staged)

---

## ❓ سوالات متداول

### Q: چرا commit من انجام نمی‌شود؟

A: احتمالاً ESLint یا Prettier خطایی پیدا کرده‌اند. خطا را در ترمینال ببینید و رفع کنید.

### Q: آیا می‌توانم hook را غیرفعال کنم؟

A: بله، اما توصیه نمی‌شود. می‌توانید فایل `.husky/pre-commit` را حذف کنید یا از `--no-verify` استفاده کنید.

### Q: چرا lint-staged فقط فایل‌های staged را بررسی می‌کند؟

A: برای سرعت بیشتر. اگر می‌خواهید همه فایل‌ها را بررسی کنید، از `pnpm lint` استفاده کنید.

### Q: چگونه می‌توانم قوانین ESLint را برای یک فایل خاص غیرفعال کنم؟

A: در بالای فایل اضافه کنید:

```javascript
/* eslint-disable rule-name */
```

یا برای یک خط:

```javascript
// eslint-disable-next-line rule-name
```

---

**نکته**: این راه‌اندازی برای یک پروژه Nuxt 3 با Vue 3 و TypeScript بهینه شده است. اگر نیاز به تغییر دارید، فایل‌های پیکربندی را ویرایش کنید.
